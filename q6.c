// Write a program which takes a character as an input and displays its ASCII code.
#include<stdio.h>
#include<conio.h>
int main()
{
    char c;
    printf("Enter the given character:");
    scanf("%c",&c);
    printf("ASCII value of the given number is %d",c,c);
    return 0;
}